import "./home.scss";
import axiosInstance from "../../api";

const Home = () => {

  return (
    <main className="home-page">
      {/* <div>
        <Carousel className="carousel" data={ImageData} />
      </div> */}
    </main>
  );
};

export default Home;
